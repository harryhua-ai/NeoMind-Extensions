# Rust
/target/
**/*.rs.bk
*.pdb

# Cargo
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
*.iml
*.bak*
*.final*
*.before_init_fix
# Build outputs
*.dylib
*.so
*.dll
*.a
*.lib
*.wasm
*.wat

# Exception: Pre-built WASM files for CI
!extensions/weather-forecast/weather_forecast_wasm.wasm

# Built .nep packages (too large for git)
dist/
*.nep

# Pre-built binaries (too large for git)
libneomind_extension_*.dylib
libneomind_extension_*.so
libneomind_extension_*.dll

# Models and data files (too large for git)
# models/ at root level is ignored
/models/
*.bin
*.pt
*.pth

# Exception: Allow ONNX models in extension directories
!extensions/*/models/*.onnx

# Profiling
*.profraw
*.profdata

# Debug
*.dSYM/
*.su
*.idb
*.pdb

# Backup files
*.bak
*.backup
*~

# Log files
*.log

# Temporary files
tmp/
temp/

# Node.js / AssemblyScript
node_modules/
package-lock.json
*.wasm
*.wat

# Exception: Pre-built WASM files for CI
!extensions/weather-forecast/weather_forecast_wasm.wasm

# Build outputs from AssemblyScript
/extensions/*/build/
/extensions/*/target/
/extensions/*/.serena/

# Test coverage
coverage/

# Serena MCP tool (cache and config)
.serena/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# GitHub Actions cache (local testing)
.github/workflows/*-cache/

# Release files (these should be in Releases, not git)
release-files/
releases/
/NeoMind

# Temporary files
*.tmp
*.tmp*
*.temp
*.bak
*.bak2
*.bak3
*.bak4
*.bak5
*.backup
*.backup2
.claude/scheduled_tasks.lock
tests/simulators/ha_mock_server.py
tests/simulators/lorawan_mqtt.py
tests/simulators/modbus_slave.py
tests/simulators/README.md
tests/simulators/requirements.txt

# Python service artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.venv*/
extensions/*/server/.venv*/

# Voice-assistant runtime secrets (NeoMind API keys, encryption material)
extensions/voice-assistant/service/data/

# Out-of-tree upstream patches (reference-only, not built)
extensions/voice-assistant/service/patches/

# Per-host latency reports (regenerated each run)
extensions/voice-assistant/service/latency_*.json

# Local runtime data (api_keys.redb, encryption_key, etc.)
/data/

# Local design docs / specs (kept locally, not committed)
docs/
.superpowers/
