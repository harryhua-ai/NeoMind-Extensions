# Runtime deps. The MOSS-TTS-Nano repo itself must be installed (`pip install -e .`)
# — see start.sh. The list below only adds what's needed for the HTTP layer.
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pydantic>=2.0
