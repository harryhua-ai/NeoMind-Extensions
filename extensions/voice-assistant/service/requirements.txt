# Runtime deps. Silero VAD ships inside sherpa-onnx.
sherpa-onnx>=1.10.0
# Optional: real server-side AEC. If absent, make_aec falls back to Noop.
webrtc-audio-processing>=0.1.0
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
numpy>=1.24
httpx>=0.27
pyyaml>=6.0
# Needed by ZipVoiceInprocTTS to load default prompt audio.
# Required only for allinone profile; harmless for HTTP-only users.
soundfile>=0.12
pytest-asyncio>=0.23
opentelemetry-sdk>=1.20.0  # optional: set VOICE_ASSISTANT_TRACE=1 to enable
